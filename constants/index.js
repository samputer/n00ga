const bells = require('./bells');
const colors = require('./colors');
const columns = require('./columns');
const fonts = require('./fonts');
const lagEffects = require('./lagEffects');
const leadEffects = require('./leadEffects');
const lines = require('./lines');
const pages = require('./pages');
const speeds = require('./speeds');
const waitTimes = require('./waitTimes');

module.exports = {
  bells,
  colors,
  columns,
  fonts,
  lagEffects,
  leadEffects,
  lines,
  pages,
  speeds,
  waitTimes,
};
